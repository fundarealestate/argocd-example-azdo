apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-azdo
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-azdo
      team: sre
  template:
    metadata:
      labels:
        app: guestbook-azdo
    spec:
      containers:
        - image: gcr.io/google-samples/gb-frontend:v5
          name: guestbook-azdo
          ports:
            - containerPort: 80
